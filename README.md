# Codable Resume

A **code-first resume** written in [Typst](https://typst.app). Edit content in `resume.typ`, run a single command, and export to PDF. No GUI required—everything is versionable and scriptable.

## Project overview

- **Input:** Profile content in `content/profiles/` + shared identity in `content/shared.typ`. Select profile at compile time with `--input profile=...`.
- **Output:** `resume-<profile>-<template>.pdf` — generated by the Typst compiler.
- **Profiles:** `tech-lead` (default), `java`, `engineering-manager` — each tailored for different job targets.
- **Workflow:** Edit the profile file for your target role → run build → open PDF. Use watch mode for live recompilation while editing.
- **Learning Typst:** See the [**docs/**](docs/README.md) folder for short guides on Typst syntax, editing this resume, and styling/layout—useful for anyone using this project in the future.

## Requirements

- **Typst CLI** (the only required dependency; no Node.js needed for compilation, but npm scripts are provided for convenience).

## Installation

### 1. Install Typst

Install the Typst compiler using one of the following methods.

**macOS (Homebrew):**
```bash
brew install typst
```

**Windows (scoop):**
```bash
scoop install typst
```

**Linux (typical):**
- Download a release from [Typst releases](https://github.com/typst/typst/releases) and put the binary in your `PATH`, or
- Build from source: `cargo install typst-cli` (requires [Rust](https://rustup.rs)).

**Verify installation:**
```bash
typst --version
```
You should see a version number (e.g. `typst 0.11.0`).

### 2. Clone or copy this project

If this is already in your `~/CodeBase` folder, you’re set. Otherwise:

```bash
cd ~/CodeBase
# If you have it in git:
# git clone <your-repo-url> codable-resume
cd codable-resume
```

### 3. (Optional) Use npm scripts

This project includes a `package.json` with `build` and `watch` scripts. You don’t need to run `npm install`—there are no npm dependencies. The scripts simply call the Typst CLI. If you prefer not to use npm, use the Typst commands directly (see “Usage” below).

## Usage

### Build PDF (one-off)

From the project root:

```bash
npm run build
```

This builds all profiles (`tech-lead`, `java`, `engineering-manager`) × templates (`modern`, `original`) × photo variants.

Build a single profile:

```bash
npm run build:java
npm run build:engineering-manager
npm run build:tech-lead
```

Or without npm:

```bash
typst compile --root . --input profile=java templates/modern.typ resume-java-modern.pdf
```

Default output aliases (backward-compatible): `resume.pdf`, `resume-modern.pdf` (tech-lead + modern).

### Watch mode (recompile on save)

```bash
npm run watch              # tech-lead (default)
npm run watch:java
npm run watch:engineering-manager
```

Save the file and the PDF will recompile automatically. Stop with `Ctrl+C` when done.

### What to edit

| What | Where |
|------|-------|
| **Name, contact, education** | `content/shared.typ` (shared across profiles) |
| **Tagline, summary, experience, skills** | `content/profiles/<profile>.typ` |
| **Layout / styling** | `templates/modern.typ` or `templates/original.typ` |

See [docs/02-editing-this-resume.md](docs/02-editing-this-resume.md) for full editing guide and how to add/remove profiles.

## Project layout

```
codable-resume/
├── README.md       # This file — installation and usage
├── assets/         # Static assets (profile photo, etc.)
│   └── profile_pic.jpg
├── content/        # Resume content (edit here)
│   ├── shared.typ          # Name, contact, education, photo (shared)
│   ├── data.typ            # Profile selector (compile-time switch)
│   └── profiles/           # One file per job-target profile
│       ├── tech-lead.typ   # Generic Tech Lead (default)
│       ├── java.typ        # Senior Java SWE / Tech Lead
│       └── engineering-manager.typ
├── docs/           # Typst learning guides (see docs/README.md)
│   ├── README.md
│   ├── 01-typst-basics.md
│   ├── 02-editing-this-resume.md
│   └── 03-styling-and-layout.md
├── package.json    # Scripts: npm run build, npm run watch, per-profile builds
├── resume.typ      # Default entry point (includes modern template)
├── templates/      # Layout variants (modern, original)
├── scripts/
│   └── build.sh    # Builds all profiles × templates × photo variants
├── resume.pdf      # Generated output (tech-lead + modern, created by build)
└── .gitignore
```

## Customization

- **Paper size:** In `resume.typ`, change `#set page(paper: "us-letter", ...)` to e.g. `paper: "a4"`.
- **Fonts:** Change `#set text(font: "Linux Libertine", ...)` to another font name if you have it installed or available to Typst.
- **Layout / styling:** Adjust the `#set page(...)`, `#set text(...)`, and `#show heading.where(...)` blocks; the “Resume body” section controls structure and can be modified for different layouts.

## Troubleshooting

- **`typst: command not found`** — Typst is not in your `PATH`. Install it using one of the methods above and ensure the binary is on `PATH`.
- **Compilation errors** — Read the error message; Typst usually points to the line. Check brackets, commas in arrays, and that strings are in double quotes.
- **Missing font** — If you see font-related errors, switch to a built-in font like `"Linux Libertine"` or remove the `font` argument to use the default.

## Links

- [Typst documentation](https://typst.app/docs/)
- [Typst web app](https://typst.app) — useful for experimenting with snippets
- [Typst GitHub](https://github.com/typst/typst)
