{
  "name": "codable-resume",
  "version": "1.0.0",
  "private": true,
  "description": "Codable resume in Typst — edit in code, export to PDF",
  "scripts": {
    "build": "typst compile resume.typ resume.pdf",
    "watch": "typst watch resume.typ resume.pdf"
  }
}
